{
  "name": "artillery-load-tests",
  "version": "1.0.0",
  "description": "Runs load tests against web services",
  "scripts": {
    "artillery": "artillery run --output reports/report.json",
    "report": "artillery report --output reports/report.html reports/report.json"
  },
  "author": "Christopher Lübbemeier",
  "license": "ISC",
  "devDependencies": {
    "artillery": "^1.7.7"
  }
}
